# 中央厨房管理系统 - 项目大纲与员工手册

## 📋 项目概述

### 项目简介
中央厨房管理系统是一个基于 Spring Boot + React 的现代化企业级餐饮管理系统，专为中央厨房运营场景设计，实现从原料采购、生产计划、质量追溯到门店配送的全流程数字化管理。

### 核心功能
- 🏭 **生产管理**: 生产计划、生产批次、生产步骤跟踪
- 📦 **库存管理**: 实时库存监控、智能补货预警
- 🔍 **质量追溯**: 全链路质量控制、批次追溯
- 🛒 **供应商管理**: 供应商评估、多维度评分体系
- 📊 **数据分析**: 实时仪表板、生产效率分析
- 👥 **用户管理**: 角色权限控制、多级用户体系
- 🔐 **安全认证**: JWT 令牌认证、企业级安全标准

### 技术栈
- **后端**: Spring Boot 3.1.5, Java 17, Spring Security, JPA/Hibernate
- **数据库**: MySQL 8.0, Flyway 迁移, HikariCP 连接池
- **缓存**: Redis + Caffeine 多级缓存
- **前端**: React 19, TypeScript, Material-UI, ECharts
- **认证**: JWT 令牌认证, BCrypt 密码加密
- **监控**: Spring Actuator, 自定义监控端点
- **文档**: OpenAPI/Swagger, 自动化 API 文档

---

## 🏗️ 技术架构

### 系统架构图
```
┌─────────────────────────────────────────────────────────────┐
│                    前端层 (React + TypeScript)                │
│  ┌─────────────────────────────────────────────────────────┐  │
│  │  组件层: Dashboard, Production, Inventory, Quality...     │  │
│  │  服务层: API 调用, 状态管理, 路由控制                     │  │
│  │  工具层: 工具函数, 常量, 类型定义                         │  │
│  └─────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────┘
                                │
                                │ HTTP/HTTPS
                                ▼
┌─────────────────────────────────────────────────────────────┐
│                    后端层 (Spring Boot)                      │
│  ┌─────────────────────────────────────────────────────────┐  │
│  │  控制器层: REST API, 请求处理, 响应格式化               │  │
│  │  服务层: 业务逻辑, 数据处理, 事务管理                    │  │
│  │  数据层: Repository, 实体映射, 查询优化                  │  │
│  │  配置层: 安全配置, 缓存配置, 数据库配置                  │  │
│  └─────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────┘
                                │
                                │ JDBC
                                ▼
┌─────────────────────────────────────────────────────────────┐
│                    数据层                                     │
│  ┌─────────────────────────────────────────────────────────┐  │
│  │  数据库: MySQL 8.0 (生产) / H2 (开发)                   │  │
│  │  缓存: Redis (分布式缓存) + Caffeine (本地缓存)         │  │
│  │  迁移: Flyway 数据库版本控制                             │  │
│  └─────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────┘
```

### 核心模块

#### 1. 用户认证与授权模块
- JWT 令牌认证
- 角色-based 权限控制
- 多级用户体系 (管理员/经理/员工)

#### 2. 生产管理模块
- 生产订单管理
- 生产计划排程
- 生产批次跟踪
- 生产步骤监控

#### 3. 库存管理模块
- 实时库存监控
- 智能补货预警
- 库存盘点功能
- 库存分析报告

#### 4. 质量追溯模块
- 原料批次追溯
- 生产过程记录
- 质量检测结果
- 不合格品处理

#### 5. 供应商管理模块
- 供应商信息管理
- 供应商评估体系
- 合同价格管理
- 交付周期监控

#### 6. 数据分析模块
- 生产效率分析
- 成本分析报告
- 质量统计图表
- 预测分析功能

---

## 📊 数据库设计

### 核心实体关系图
```
Users (用户)
├── id (主键)
├── username (用户名, 唯一)
├── password (密码, 加密)
├── email (邮箱, 唯一)
├── role (角色: ADMIN/MANAGER/OPERATOR)
└── enabled (状态)

ProductionOrders (生产订单)
├── id (主键)
├── order_number (订单号, 唯一)
├── franchise_id (加盟商ID)
├── production_standard_id (生产标准ID)
├── quantity (数量)
├── unit_price (单价)
├── total_amount (总金额, 计算字段)
├── priority (优先级)
├── status (状态)
└── timestamps (时间戳)

ProductionBatches (生产批次)
├── id (主键)
├── batch_number (批次号, 唯一)
├── production_order_id (订单ID)
├── planned_quantity (计划数量)
├── actual_quantity (实际数量)
├── start_time/end_time (开始/结束时间)
├── status (状态)
└── quality_notes (质量备注)

QualityTrace (质量追溯)
├── id (主键)
├── batch_number (批次号)
├── ingredient_id (原料ID)
├── production_date (生产日期)
├── expiry_date (过期日期)
├── supplier_info (供应商信息)
├── quality_check (质量检查结果)
└── status (状态)

Suppliers (供应商)
├── id (主键)
├── name (供应商名称)
├── category (分类)
├── quality_grade (质量等级)
├── contract_price (合同价格)
├── delivery_cycle (交付周期)
├── rating (评分)
└── status (状态)

Franchises (加盟商)
├── id (主键)
├── name (加盟商名称)
├── contact_person (联系人)
├── contact_phone (联系电话)
├── address (地址)
└── status (状态)
```

### 数据库优化策略

#### 索引设计
```sql
-- 主键索引 (自动创建)
-- 唯一索引 (username, email, order_number, batch_number)
-- 复合索引
CREATE INDEX idx_production_orders_status_date ON production_orders(status, order_date);
CREATE INDEX idx_production_batches_status_time ON production_batches(status, start_time, end_time);
CREATE INDEX idx_quality_trace_expiry_status ON quality_trace(expiry_date, status);
CREATE INDEX idx_users_role_enabled ON users(role, enabled);
```

#### 性能优化
- **连接池**: HikariCP 配置优化
- **缓存策略**: 多级缓存 (Redis + Caffeine)
- **查询优化**: 自定义 Repository 方法
- **分页查询**: 所有列表接口支持分页

---

## 🔐 安全认证系统

### JWT 认证流程
```
1. 用户登录请求
   POST /api/auth/login
   Body: { "username": "admin", "password": "password" }

2. 服务器验证
   - 检查用户名密码
   - 生成 JWT 令牌
   - 返回用户信息和令牌

3. 后续请求认证
   Authorization: Bearer <JWT_TOKEN>

4. 服务器验证令牌
   - 解析 JWT 令牌
   - 验证签名和过期时间
   - 设置安全上下文
```

### 安全配置
```yaml
# JWT 配置
ckm:
  security:
    jwt:
      secret: ${JWT_SECRET:mySecretKey...}
      expiration: 86400000  # 24小时

# Spring Security 配置
spring:
  security:
    filter:
      order: 10
```

### 权限控制
- **方法级安全**: `@PreAuthorize("hasRole('ADMIN')")`
- **URL 级安全**: 配置路径访问权限
- **数据级安全**: 基于用户的资源过滤

---

## 🚀 部署运维

### 环境要求
- **Java**: JDK 17+
- **数据库**: MySQL 8.0+
- **缓存**: Redis 6.0+
- **构建工具**: Maven 3.8+

### 部署架构
```
生产环境部署架构:
┌─────────────────────────────────────────────────────────────┐
│                    Load Balancer (Nginx)                     │
├─────────────────────────────────────────────────────────────┤
│                Application Servers                          │
│  ┌─────────────────────────────────────────────────────────┐  │
│  │  App Server 1    App Server 2    App Server 3          │  │
│  │  ├─────────────┼─────────────┼─────────────┤           │  │
│  │  │ Spring Boot │ Spring Boot │ Spring Boot │           │  │
│  │  └─────────────┴─────────────┴─────────────┘           │  │
│  └─────────────────────────────────────────────────────────┘  │
├─────────────────────────────────────────────────────────────┤
│                Cache Layer (Redis Cluster)                   │
├─────────────────────────────────────────────────────────────┤
│                Database Layer (MySQL Cluster)                │
└─────────────────────────────────────────────────────────────┘
```

### Docker 部署
```dockerfile
# Dockerfile
FROM openjdk:17-jdk-slim
COPY target/central-kitchen-management-0.0.1-SNAPSHOT.jar app.jar
EXPOSE 8081
ENTRYPOINT ["java","-jar","/app.jar"]
```

```yaml
# docker-compose.yml
version: '3.8'
services:
  app:
    build: .
    ports:
      - "8081:8081"
    environment:
      - SPRING_PROFILES_ACTIVE=prod
      - DB_HOST=mysql
      - REDIS_HOST=redis
    depends_on:
      - mysql
      - redis

  mysql:
    image: mysql:8.0
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: ckm_prod

  redis:
    image: redis:6.0-alpine
```

---

## 📚 API 接口文档

### 认证接口
```http
# 用户登录
POST /api/auth/login
Content-Type: application/json

{
  "username": "admin",
  "password": "password"
}

# 响应
{
  "success": true,
  "data": {
    "token": "eyJhbGciOiJIUzI1NiJ9...",
    "tokenType": "Bearer",
    "expiresIn": 86400,
    "user": {
      "id": 1,
      "username": "admin",
      "role": "ADMIN"
    }
  }
}
```

### 用户管理接口
```http
# 获取用户列表
GET /api/users?page=0&size=10
Authorization: Bearer <token>

# 创建用户
POST /api/users
Authorization: Bearer <token>
Content-Type: application/json

{
  "username": "newuser",
  "password": "password123",
  "email": "user@example.com",
  "role": "OPERATOR"
}
```

### 生产管理接口
```http
# 创建生产订单
POST /api/production/orders
Authorization: Bearer <token>

{
  "franchiseId": 1,
  "productionStandardId": 1,
  "quantity": 100,
  "requiredDate": "2024-01-15"
}

# 获取生产批次列表
GET /api/production/batches?status=IN_PROGRESS
Authorization: Bearer <token>
```

---

## 🧪 测试策略

### 测试金字塔
```
┌─────────────────────────────────────────────────────────────┐
│                    E2E Tests (端到端测试)                     │
│                    Integration Tests (集成测试)              │
├─────────────────────────────────────────────────────────────┤
│                    Unit Tests (单元测试)                     │
│                                                            │
│  Service Tests    Repository Tests    Controller Tests     │
│  ├────────────────┼─────────────────┼─────────────────┤     │
│  │ Business Logic │ Data Access     │ HTTP Requests   │     │
│  └────────────────┴─────────────────┴─────────────────┘     │
└─────────────────────────────────────────────────────────────┘
```

### 测试技术栈
- **单元测试**: JUnit 5 + Mockito
- **集成测试**: Spring Boot Test + TestContainers
- **API 测试**: RestAssured
- **前端测试**: Jest + React Testing Library

### 测试覆盖率目标
- **单元测试**: 80%+ 行覆盖率
- **集成测试**: 主要业务流程
- **E2E 测试**: 关键用户流程

---

## 👥 员工职责分工

### 开发团队

#### 项目经理 (Project Manager)
**职责**:
- 项目计划制定和跟踪
- 团队协调和沟通
- 需求分析和确认
- 风险管理和控制
- 质量保证和交付管理

**技能要求**:
- 项目管理经验 3+ 年
- 软件开发流程了解
- 沟通协调能力强
- 问题解决能力优秀

#### 技术架构师 (Technical Architect)
**职责**:
- 系统架构设计
- 技术选型和评估
- 代码规范制定
- 性能优化指导
- 技术债务管理

**技能要求**:
- Java/Spring 开发经验 5+ 年
- 架构设计经验丰富
- 多种技术栈熟悉
- 性能优化经验

#### 后端开发工程师 (Backend Developer)
**职责**:
- 后端 API 开发
- 数据库设计和优化
- 业务逻辑实现
- 单元测试编写
- 代码审查

**技能要求**:
- Java/Spring Boot 熟练
- SQL 数据库设计经验
- RESTful API 设计
- 单元测试编写

#### 前端开发工程师 (Frontend Developer)
**职责**:
- React 组件开发
- 用户界面设计
- 前后端接口联调
- 前端性能优化
- 用户体验优化

**技能要求**:
- React/TypeScript 熟练
- HTML/CSS/JavaScript 精通
- UI/UX 设计理念
- 响应式设计经验

#### 测试工程师 (QA Engineer)
**职责**:
- 测试用例设计
- 自动化测试脚本编写
- 缺陷跟踪和管理
- 性能测试执行
- 质量分析报告

**技能要求**:
- 软件测试理论知识
- 自动化测试工具熟练
- 缺陷管理经验
- 质量分析能力

#### DevOps 工程师 (DevOps Engineer)
**职责**:
- CI/CD 流程建设
- 服务器环境管理
- 监控告警配置
- 自动化部署脚本
- 性能监控和优化

**技能要求**:
- Linux 服务器管理
- Docker/K8s 经验
- CI/CD 工具使用
- 脚本编写能力

### 运营团队

#### 系统管理员 (System Administrator)
**职责**:
- 服务器硬件维护
- 操作系统管理
- 网络安全配置
- 备份恢复管理
- 性能监控调优

**技能要求**:
- Linux 系统管理经验
- 网络安全知识
- 数据库维护经验
- 问题诊断能力

#### 数据分析师 (Data Analyst)
**职责**:
- 业务数据分析
- 报表制作和优化
- 数据质量监控
- 用户行为分析
- 业务指标跟踪

**技能要求**:
- SQL 数据查询分析
- 数据可视化工具
- 统计分析方法
- 业务理解能力

#### 技术支持工程师 (Technical Support)
**职责**:
- 用户问题解答
- 故障排查和解决
- 文档编写和维护
- 培训材料准备
- 用户培训执行

**技能要求**:
- 技术文档编写
- 问题诊断能力
- 用户沟通技巧
- 培训组织经验

---

## 📋 开发规范

### 代码规范

#### Java 代码规范
```java
// 类命名: PascalCase
public class UserService {
    // 常量: UPPER_SNAKE_CASE
    private static final String DEFAULT_ROLE = "USER";

    // 变量: camelCase
    private String username;

    // 方法: camelCase
    public UserDTO findByUsername(String username) {
        // 参数验证
        if (username == null || username.trim().isEmpty()) {
            throw new IllegalArgumentException("用户名不能为空");
        }

        // 业务逻辑
        return userRepository.findByUsername(username)
            .map(userMapper::toDTO)
            .orElseThrow(() -> new ResourceNotFoundException("用户不存在"));
    }
}
```

#### 注释规范
```java
/**
 * 用户服务类
 * 提供用户相关的业务逻辑处理
 *
 * @author 系统开发团队
 * @version 1.0.0
 */
@Service
public class UserService {

    /**
     * 根据用户名查找用户
     *
     * @param username 用户名，不能为空
     * @return 用户DTO对象
     * @throws ResourceNotFoundException 当用户不存在时抛出
     */
    @Cacheable(value = "user", key = "'username:' + #username")
    public UserDTO findByUsername(String username) {
        // 业务逻辑实现
    }
}
```

### 提交规范

#### Git 提交信息格式
```
<type>(<scope>): <subject>

<body>

<footer>
```

#### 提交类型
- `feat`: 新功能
- `fix`: 修复bug
- `docs`: 文档更新
- `style`: 代码格式调整
- `refactor`: 代码重构
- `test`: 测试相关
- `chore`: 构建工具配置

#### 提交示例
```
feat(user): 添加用户注册功能

- 实现用户注册API接口
- 添加邮箱验证逻辑
- 完善错误处理机制

Closes #123
```

### API 设计规范

#### RESTful 设计原则
```http
# 资源命名: 复数形式
GET    /api/users          # 获取用户列表
GET    /api/users/{id}     # 获取指定用户
POST   /api/users          # 创建用户
PUT    /api/users/{id}     # 更新用户
DELETE /api/users/{id}     # 删除用户

# 嵌套资源
GET    /api/users/{id}/orders    # 获取用户订单
POST   /api/orders/{id}/items    # 为订单添加商品

# 查询参数
GET    /api/users?page=1&size=10&sort=name,asc
GET    /api/users?role=ADMIN&enabled=true
```

#### 响应格式规范
```json
// 成功响应
{
  "success": true,
  "data": {
    "id": 1,
    "username": "admin",
    "email": "admin@example.com"
  },
  "timestamp": "2024-01-12T10:00:00Z"
}

// 错误响应
{
  "success": false,
  "message": "用户名已存在",
  "errorCode": "USER_EXISTS",
  "timestamp": "2024-01-12T10:00:00Z"
}

// 分页响应
{
  "success": true,
  "data": {
    "content": [...],
    "pageable": {
      "page": 0,
      "size": 10,
      "sort": ["name,asc"]
    },
    "totalElements": 100,
    "totalPages": 10,
    "first": true,
    "last": false
  }
}
```

---

## 🔄 工作流程

### 需求开发流程
```
1. 需求提出
   ↓
2. 需求分析评审
   ↓
3. 技术方案设计
   ↓
4. 任务拆分分配
   ↓
5. 代码开发实现
   ↓
6. 单元测试编写
   ↓
7. 代码审查 (Code Review)
   ↓
8. 集成测试
   ↓
9. 部署验证
   ↓
10. 需求交付
```

### 代码审查 Checklist
- [ ] 代码符合编码规范
- [ ] 单元测试覆盖完整
- [ ] 异常处理完善
- [ ] 性能考虑充分
- [ ] 安全漏洞检查
- [ ] 文档更新完整

### 部署流程
```
开发环境 → 测试环境 → 预发布环境 → 生产环境

1. 代码提交到 Git
2. CI/CD 自动构建
3. 自动化测试执行
4. 人工审核通过
5. 自动部署到目标环境
6. 健康检查验证
7. 流量切换完成
```

---

## 📞 联系方式

### 技术支持
- **内部技术支持**: tech-support@company.com
- **紧急联系人**: emergency@company.com
- **项目经理**: pm@company.com

### 文档资源
- **项目文档**: https://docs.company.com/ckm
- **API 文档**: http://localhost:8081/swagger-ui.html
- **代码仓库**: https://github.com/company/central-kitchen-management

---

## 📈 项目里程碑

### Phase 1: 基础功能 (已完成)
- [x] 用户认证系统
- [x] 基础CRUD操作
- [x] 数据库设计
- [x] 前端界面框架

### Phase 2: 核心功能 (进行中)
- [x] 生产管理模块
- [x] 库存管理模块
- [x] 质量追溯模块
- [ ] 供应商管理模块
- [ ] 数据分析模块

### Phase 3: 高级功能 (规划中)
- [ ] 移动端支持
- [ ] 第三方集成
- [ ] AI 预测分析
- [ ] 区块链追溯
- [ ] 国际化支持

---

## 🎯 总结

中央厨房管理系统是一个现代化的企业级应用，具有以下特点：

### 技术先进性
- 采用最新的技术栈 (Spring Boot 3, React 19, Java 17)
- 微服务架构设计，支持水平扩展
- 多级缓存策略，性能优化
- 完整的监控和日志体系

### 功能完整性
- 涵盖中央厨房运营的全业务流程
- 模块化设计，功能独立可扩展
- 标准化 API 接口，便于集成

### 安全可靠性
- 企业级安全认证和授权
- 完善的异常处理和错误恢复
- 数据备份和灾难恢复机制

### 用户体验
- 现代化响应式前端界面
- 直观的操作流程和用户引导
- 实时数据展示和交互反馈

### 运维友好
- 容器化部署，支持自动化运维
- 完整的监控和告警体系
- 详细的文档和培训材料

这个项目不仅是一个技术实现，更是一个完整的数字化转型解决方案，为中央厨房运营提供了强有力的技术支撑。

---

*本文档由系统开发团队维护，如有疑问请联系技术支持团队。*
